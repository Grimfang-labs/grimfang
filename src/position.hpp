#pragma once

#include "types.hpp"

// ===========================================================================
// position.hpp - board state (implemented in sub-step 2).
// This sub-step-1 placeholder only declares the type so the project links.
// ===========================================================================

class Position {
    // Implemented in sub-step 2: hybrid bitboard/mailbox board, FEN I/O,
    // make/unmake, attackers/checkers/pins.
};
